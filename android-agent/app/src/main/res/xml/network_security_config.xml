<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      Certificate pinning for the MDM backend. Replace the SHA-256 pins with
      the real ones produced from your TLS cert chain (intermediate + leaf).
      Pin both so cert rotation does not brick the fleet.
    -->
    <domain-config>
        <domain includeSubdomains="true">mdm.example.com</domain>
        <pin-set expiration="2030-01-01">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>
</network-security-config>
